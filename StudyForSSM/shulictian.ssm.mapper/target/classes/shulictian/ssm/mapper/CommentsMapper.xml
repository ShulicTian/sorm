<?xml version="1.0" encoding="GB18030"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="shulictian.ssm.mapper.CommentsMapper">

	<!-- 储存评论 -->
	<insert id="saveComment" parameterType="map">
		insert into comments (content,top_id，user_id) value(#{content},#{topId},#{userId})
	</insert>

	<!-- 获取帖子对应评论 -->
	<select id="getComments" parameterType="int" resultType="comments">
		select * from comments where top_id=#{id}
	</select>
	
	<!-- 删除此评论 -->
	<delete id="delComment" parameterType="map">
		delete from comments where top_id=#{topId} and id=#{id}
	</delete>
	
</mapper>